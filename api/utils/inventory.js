const inventory = function (obj, arr) {
  //TODO : obj of product + array of ingredient
  // for each product, fetch what ingredient compose him and count all used ingredients + array
  // current stock = currStock - usedIngredients
};

module.exports = bill;
